# ── secrets ──────────────────────────────────────────────────────────────
# .env دارد کلید ادمین، JWT secret و رمز object storage. مقادیر نمونه در
# .env.example هستند و همان‌ها commit می‌شوند.
.env
.env.local
*.env.local
backend/.env
web/.env.local

# ── build output ─────────────────────────────────────────────────────────
node_modules/
web/.next/
web/out/
web/next-env.d.ts

# باینری‌های Go
backend/bin/
backend/api
backend/worker
backend/scheduler

# ── ابزار ─────────────────────────────────────────────────────────────────
.DS_Store
*.log
.idea/
.vscode/
